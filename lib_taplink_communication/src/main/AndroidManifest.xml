<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- USB权限 -->
    <uses-feature android:name="android.hardware.usb.host" />
    <uses-feature android:name="android.hardware.usb.accessory" />
    <uses-permission android:name="android.hardware.usb.accessory" />
    <uses-permission android:name="android.permission.USB_PERMISSION" />
    
    <!-- 网络权限（WebSocket） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application>
        <!-- Local IPC service -->

        <!--
            AOA device claim entry point. Declaring a <usb-device> filter makes the
            system grant this app USB permission for the device automatically (no
            prompt) once the peer switches into accessory mode.
            This must be an Activity: ACTION_USB_DEVICE_ATTACHED is dispatched by the
            system as an Activity Intent, so neither runtime nor manifest
            BroadcastReceivers ever see it. See the class KDoc for details.
        -->
        <activity
            android:name="com.sunmi.tapro.taplink.communication.cable.aoa.host.TaplinkUsbDeviceAttachedActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/taplink_usb_device_filter" />
        </activity>
    </application>

</manifest>



